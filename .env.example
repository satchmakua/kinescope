# Copy to `.env` and fill in real values. `.env` is gitignored; this file is not.
#
# Kinescope's core, tests, and the bundled demo need NO secrets — they run fully offline.
# An API key is only needed if you point an example at the real Anthropic API instead
# of a stub transport.

# ANTHROPIC_API_KEY=sk-ant-...
